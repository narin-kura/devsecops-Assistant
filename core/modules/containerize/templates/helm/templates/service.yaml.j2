apiVersion: v1
kind: Service
metadata:
  name: [[ service_name ]]
  labels:
    app: [[ service_name ]]
spec:
  type: {{ .Values.service.type }}
  selector:
    app: [[ service_name ]]
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      protocol: TCP
